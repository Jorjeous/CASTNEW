run next line. NeMo must be installed (nemo_toolkit[all])

python align.py pretrained_name="stt_en_fastconformer_hybrid_large_pc" manifest_filepath="<manifest with audio>" output_dir="<where to put files>"
